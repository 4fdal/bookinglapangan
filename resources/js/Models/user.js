export default class User {
    constructor({ name, email, no_ponsel, password }) {
        this.name = name;
        this.email = email;
        this.no_ponsel = no_ponsel;
        this.password = password;
    }
}
